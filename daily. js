cd /home/claude/fish-it-gileg && python3 - <<'EOF'
with open('style.css','r') as f:
    css = f.read()

# Add new styles at end
new_styles = """
/* ══════════════════════════
   COMBO BADGE
══════════════════════════ */
.combo-badge{
  width:100%;text-align:center;
  font-family:'Fredoka One',cursive;font-size:1.2rem;
  color:#ff8800;
  text-shadow:0 0 12px rgba(255,150,0,.6);
  animation:comboPop .35s cubic-bezier(.34,1.56,.64,1);
  flex-shrink:0;
}
@keyframes comboPop{from{transform:scale(.5);opacity:0}to{transform:scale(1);opacity:1}}

/* ══════════════════════════
   ACHIEVEMENTS
══════════════════════════ */
.ach-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(155px,1fr));gap:9px;padding:12px;}
.ach-card{background:var(--bg-card);border-radius:var(--br);border:1px solid rgba(255,255,255,.07);
  padding:13px;display:flex;flex-direction:column;gap:5px;transition:transform .2s;}
.ach-card.ach-done{border-color:rgba(255,215,0,.4);background:rgba(255,215,0,.04);}
.ach-card:hover{transform:translateY(-2px);}
.ach-ico{font-size:2rem;text-align:center;}
.ach-nm{font-size:.8rem;font-weight:900;text-align:center;}
.ach-desc{font-size:.65rem;color:rgba(255,255,255,.4);text-align:center;line-height:1.4;}
.ach-rwd{font-size:.7rem;font-weight:800;color:var(--gold);text-align:center;}
.ach-bar{height:5px;background:rgba(255,255,255,.08);border-radius:5px;overflow:hidden;}
.ach-fill{height:100%;background:linear-gradient(90deg,#2ecc40,#aaff44);border-radius:5px;transition:width .5s;}
.ach-prog{font-size:.6rem;color:rgba(255,255,255,.35);text-align:right;}

/* ══════════════════════════
   DAILY LOGIN MODAL
══════════════════════════ */
.login-streak-dots{display:flex;gap:5px;justify-content:center;flex-wrap:wrap;margin:4px 0;}
.login-dot{width:30px;height:30px;border-radius:50%;border:2px solid rgba(255,215,0,.3);
  display:flex;align-items:center;justify-content:center;font-size:.7rem;font-weight:800;color:rgba(255,255,255,.3);}
.login-dot.ld-done{background:linear-gradient(135deg,#ffe144,#ff8800);border-color:var(--gold);color:#1a0a00;}

/* ══════════════════════════
   SHOP item owned badge
══════════════════════════ */
.btn-buy.btn-owned{background:rgba(46,204,64,.15);border:1px solid rgba(46,204,64,.3);color:#2ecc40;cursor:default;}

/* ══════════════════════════
   EXTRA responsive for achievements
══════════════════════════ */
@media(max-width:420px){
  .ach-grid{grid-template-columns:1fr 1fr;}
}
@media(max-height:500px) and (orientation:landscape){
  .ach-grid{grid-template-columns:repeat(4,1fr);}
}
"""
css += new_styles
with open('style.css','w') as f:
    f.write(css)
print("✅ Styles added")
EOF